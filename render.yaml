services:
  - type: web
    name: market-intelligence-app
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn main:app --chdir web --bind 0.0.0.0:$PORT
    envVars:
      - key: SECRET_KEY
        value: dfbbirbgiu348fdugkjfg;jbskjgbkjdsbfgjbberii3
    plan: free
    autoDeploy: true
