services:
  - type: web
    name: market-intelligence-app
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn web.main:app --bind 0.0.0.0:$PORT
    envVars:
      - key: SECRET_KEY
        value: your-secret-key
    plan: free
    autoDeploy: true
